// Demo catalog for t07-drop — placeholder; curated catalog lands with the template rebuild.
import { FALLBACK_SET, type DemoSet } from "../fallback";

const set: DemoSet = FALLBACK_SET;
export default set;
