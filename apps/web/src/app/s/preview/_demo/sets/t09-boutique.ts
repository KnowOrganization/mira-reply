// Demo catalog for t09-boutique — placeholder; curated catalog lands with the template rebuild.
import { FALLBACK_SET, type DemoSet } from "../fallback";

const set: DemoSet = FALLBACK_SET;
export default set;
