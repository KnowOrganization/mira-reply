// Demo catalog for t10-typo — placeholder; curated catalog lands with the template rebuild.
import { FALLBACK_SET, type DemoSet } from "../fallback";

const set: DemoSet = FALLBACK_SET;
export default set;
