"use client";
// Shim — the skeleton system now lives in ./skeleton/. This re-export keeps the
// old `import { ViewSkeleton } from "./Skeletons"` path working. Prefer importing
// from "@/components/skeleton" directly in new code.
export * from "./skeleton";
